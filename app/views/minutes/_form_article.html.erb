<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :size => "80x3" %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%# f.label :minute_id %>
    <%# f.select :minute_id , Minute.all.collect {|m| [m.title+"(#{m.scheduled_at.strftime("%m/%d")})", m.id] }, {:include_blank=>'None'}  %>
    <%= f.hidden_field :minute_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
